# general
XPLAN_USER=dbryce
XPLAN_EMAIL=dbryce@sift.net
XPLAN_DOCKER_NAMESPACE=danbryce

# shared naming
XPLAN_BASE_PATH=xplan-${XPLAN_USER}

# remote
REMOTE_WORK_DIR=data-tacc-work-${XPLAN_USER}

#
# xplan design app
#
APP_NAME=${XPLAN_USER}_xplan2_design
APP_DEPLOYMENT_SYSTEM=data-tacc-work-${XPLAN_USER}
APP_EXECUTION_SYSTEM=hpc-tacc-wrangler-${XPLAN_USER}
APP_VERSION=0.0.1
APP_BASE_PATH=${XPLAN_BASE_PATH}

# docker
APP_DOCKER_NAMESPACE=${XPLAN_DOCKER_NAMESPACE}
APP_DOCKER_REPO=xplan2_design
APP_DOCKER_TAG=${APP_VERSION}

#
# xplan reactor
#
REACTOR_NAME=xplan-${XPLAN_USER}
REACTOR_ALIAS=${REACTOR_NAME}
REACTOR_BASE_PATH=${REACTOR_NAME}

# docker
REACTOR_DOCKER_HUB_ORG=${XPLAN_DOCKER_NAMESPACE}
REACTOR_DOCKER_IMAGE_TAG=${REACTOR_NAME}
REACTOR_DOCKER_IMAGE_VERSION=2.0